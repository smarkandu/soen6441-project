package ca.concordia.soen6441.project.interfaces;

public interface ValidateMap {
    /**
     * Checks if the map is valid or not based on several aspects
     *
     * @return True if the map is valid, else False
     */
    boolean isMapValid();
}
